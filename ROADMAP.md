# Roadmap — vterm

План развития проекта по фазам, от минимального MVP к полноценному релизу.
Каждая фаза заканчивается осязаемым артефактом, который можно показать.

**Легенда:** ✅ сделано · 🔜 в работе / следующее · ⬜ запланировано

| Фаза | Содержание | Статус |
|------|-----------|--------|
| 0 | Каркас приложения | ✅ Завершено |
| 1 | MVP: SSH-подключение и живой терминал | ✅ Завершено |
| 2 | Безопасное хранение паролей и SSH-ключей | ✅ Реализовано* |
| 3 | SFTP и передача файлов | ✅ Реализовано* |
| 4 | Полировка UX | ✅ Реализовано |
| 5 | Тесты и покрытие | ✅ Реализовано |
| 6 | Рефакторинг: безопасность, архитектура, оптимизация | ✅ Реализовано |
| 7 | Дизайн и UX-ревизия | ✅ Реализовано |
| 8 | Документация (промежуточная) | ✅ Реализовано |
| 9 | Расширение мониторинга (промежуточная) | ✅ Реализовано |
| 10 | Умная работа с логами и текстом | ✅ Реализовано |
| 11 | Аудит и запись сессий | ✅ Реализовано |
| 12 | Продвинутый SFTP и редактор конфигов | 🟦 В работе (12.1–12.2 ✅) |
| 13 | Актуализация и оформление README | ⬜ |
| 14 | Сборка и CI/CD (релиз) | ⬜ |

---

## 🔁 Принцип сборки: два файла на каждом этапе

Один файл, работающий и на Windows, и на macOS, **невозможен** (разные форматы
исполняемых файлов). Поэтому **на каждой фазе собираются два артефакта**:

- **macOS** — `.app` (портативный бандл) + `.dmg`;
- **Windows** — `.msi` / NSIS `.exe` (+ портативный `vterm.exe`).

**Definition of Done каждой фазы** включает зелёную CI-сборку обеих ОС. Механизм —
**GitLab CI** [.gitlab-ci.yml](.gitlab-ci.yml) (задачи `build:macos` + `build:windows`):
push в дефолтную ветку/MR/ручной запуск → артефакты пайплайна; push тега `v*` →
GitLab Release для обеих ОС.

> Локально доступна сборка только под текущую ОС (`pnpm tauri build`). Windows-файл
> на macOS не собрать — для него нужен Windows-раннер из CI.

---

## ✅ Фаза 0 — Каркас (завершено)

**Цель:** окно с двумя панелями запускается на macOS и Windows.

- [x] Инициализация проекта Tauri 2 + SvelteKit (Svelte 5) + TypeScript.
- [x] Подключение Tailwind CSS v4 (плагин Vite, токены темы через `@theme`).
- [x] Переименование шаблона `_scaffold` → `vterm` (package.json, Cargo.toml,
      `tauri.conf.json`, заголовок и размер окна 1100×720).
- [x] Двухпанельная вёрстка: верхнее меню (**+ Add server / Delete**), список
      серверов слева, область терминала-заглушки справа, модалка добавления сервера.
- [x] Модель данных `ServerProfile` / `NewServerProfile`
      ([src-tauri/src/model.rs](src-tauri/src/model.rs)), `serde camelCase` ↔
      TS-типы ([src/lib/types.ts](src/lib/types.ts)).
- [x] Заглушки команд бэкенда: `list_servers`, `add_server`, `delete_server`,
      `connect_session` поверх in-memory-стора ([src-tauri/src/lib.rs](src-tauri/src/lib.rs)).
- [x] Типизированные обёртки над `invoke` ([src/lib/api.ts](src/lib/api.ts)).
- [x] Проверки: `pnpm check` (0 ошибок/предупреждений), `pnpm build`,
      `cargo check`, успешный запуск `pnpm tauri dev`.
- [x] **CI-сборка** macOS + Windows (GitLab CI, [.gitlab-ci.yml](.gitlab-ci.yml)).
- [x] **Локальная macOS-сборка** выполнена: `vterm.app` (8.3 МБ) + `vterm_0.1.0_aarch64.dmg` (2.9 МБ).

**Артефакт:** приложение открывается, видна структура, можно добавлять/удалять
серверы в списке (в памяти). Реального подключения ещё нет.

**Сборка дистрибутивов:**
- ✅ macOS — собрано локально (`.app` + `.dmg`).
- ⏳ Windows — будет собрано в CI после публикации репозитория в GitLab
  (на macOS не компилируется).

---

## ✅ Фаза 1 — MVP: SSH-подключение и живой терминал

**Цель:** выбрал сервер → ввёл пароль → работаешь в живом shell (vim, htop, цвета).
Это ядро продукта и самая технически нетривиальная часть.

> **Статус:** завершено и проверено на реальном SSH-сервере (подключение, ввод/вывод,
> цвета работают).

Реализовано на `russh` **0.61** (используется `Channel::split()` —
read-half читает вывод, write-half шлёт ввод/resize, что снимает конфликт
заимствований).

### Бэкенд (Rust) — [src-tauri/src/ssh.rs](src-tauri/src/ssh.rs), [store.rs](src-tauri/src/store.rs), [lib.rs](src-tauri/src/lib.rs)
- [x] `russh` + `tokio`; SSH-сессия, аутентификация по паролю.
- [x] Удалённый PTY (`request_pty`) и shell (`request_shell`).
- [x] Мост PTY ↔ фронт:
  - [x] входящие байты с сервера → Tauri-событие `term://out/{id}`;
  - [x] нажатия клавиш из xterm.js → команда `write_to_terminal`;
  - [x] команда `resize_pty(cols, rows)` при изменении размера окна.
- [x] Управление сессиями: `HashMap<id, SshSession>` в `tauri::State` (+ команда `disconnect`).
- [x] **Персистентность профилей**: JSON в конфиг-каталоге (`directories` + `serde_json`).
- [x] Базовая обработка ошибок (отказ аутентификации, недоступность, закрытие сессии
      → событие `term://closed/{id}`).
- [x] Таймаут подключения (10 с вместо ~75 с системного TCP) и keepalive
      (15 с × 3 — обрыв «мёртвого» соединения детектируется за ~45 с).

### Фронтенд (Svelte) — [src/lib/Terminal.svelte](src/lib/Terminal.svelte), [+page.svelte](src/routes/+page.svelte)
- [x] `@xterm/xterm` 6 + `@xterm/addon-fit`, монтирование в правую панель.
- [x] Подписка на поток вывода (событие) и отправка ввода (команда).
- [x] Диалог ввода пароля при подключении.
- [x] Индикатор статуса соединения (Connecting/Connected/Disconnected/Error).

**Артефакт:** полноценная работа в удалённом shell. Это уже рабочий продукт.

**Сборка:** оба дистрибутива (macOS `.app`/`.dmg` + Windows `.msi`/`.exe`) — зелёная
CI-сборка на этом этапе.

---

## ✅ Фаза 2 — Безопасное хранение паролей и SSH-ключей

**Цель:** не вводить пароль при каждом подключении; поддержать вход по SSH-ключу.

> **Статус:** код реализован, бэкенд/фронтенд собираются без ошибок, приложение
> запускается. На `keyring` **3.6** (macOS Keychain / Windows Credential Manager).
> Осталась функциональная проверка на реальном сервере (сохранение в keychain,
> вход по ключу).

### Пароли — [secrets.rs](src-tauri/src/secrets.rs), [lib.rs](src-tauri/src/lib.rs)
- [x] Интеграция `keyring`: пароль в Keychain / Credential Manager
      (сервис `vterm:password`, аккаунт = id сервера).
- [x] Чекбокс «Сохранить в keychain» в диалоге подключения; флаг `hasSavedPassword`.
- [x] При подключении — `connect_plan` проверяет keychain; промпт только если секрета нет.
- [x] Команда `forget_secrets` — удаление сохранённых секретов (в т.ч. при удалении сервера).
- [x] **Грейсфул-обработка протухшего секрета:** секрет пишется в keychain только
      **после** успешного входа; при отказе аутентификации (`auth-rejected`) сохранённый
      секрет забывается и снова показывается запрос (сетевые ошибки секрет не трогают);
      кнопка **«Forget saved secret»** в форме редактирования.

### Аутентификация по SSH-ключу — [ssh.rs](src-tauri/src/ssh.rs)
- [x] В форме сервера — выбор **метода аутентификации**: пароль / SSH-ключ
      (поле `authMethod` в `ServerProfile`).
- [x] Поле **«путь к приватному ключу»** с нативным выбором файла
      (`tauri-plugin-dialog`); путь хранится в профиле (`keyPath`).
- [x] **Passphrase** к ключу — хранится в `keyring` (сервис `vterm:passphrase`),
      запрашивается только для зашифрованного ключа (`key_is_encrypted`).
- [x] russh: `authenticate_publickey` + `load_secret_key`, авто-подбор RSA-хэша
      (`best_supported_rsa_hash`); ed25519 / rsa / ecdsa.
- [ ] (Опционально) **встроенное хранилище ключей**: импорт ключа в каталог приложения,
      список ключей и привязка к серверам, копирование публичного ключа в буфер
      (для `ssh-copy-id`), генерация новой пары ключей.

### Опционально
- [ ] Портативное зашифрованное хранилище под мастер-паролем (`chacha20poly1305`) —
      для синхронизации между машинами.

**Артефакт:** пароли и ключи в системном безопасном хранилище; автоподключение по
паролю и по SSH-ключу.

**Сборка:** оба дистрибутива (macOS + Windows) — зелёная CI-сборка на этом этапе.

---

## ✅ Фаза 3 — SFTP и передача файлов

**Цель:** видеть удалённые файлы и обмениваться ими.

> **Статус:** код реализован, бэкенд/фронтенд собираются без ошибок, приложение
> запускается. Осталась функциональная проверка на реальном сервере.

- [x] `russh-sftp` поверх существующей SSH-сессии (отдельный subsystem-канал,
      открывается лениво — [ssh.rs](src-tauri/src/ssh.rs) `SshSession::sftp()`).
- [x] Команды: `sftp_home`, `sftp_list`, `sftp_mkdir`, `sftp_delete`,
      `sftp_upload`, `sftp_download` ([sftp.rs](src-tauri/src/sftp.rs)).
- [x] UI файлового менеджера: панель справа от терминала (кнопка **Files**),
      список директории, навигация по папкам, «вверх», обновление, новая папка,
      удаление с подтверждением ([SftpPanel.svelte](src/lib/SftpPanel.svelte)).
- [x] Загрузка **перетаскиванием** (drag-and-drop) через `onDragDropEvent` Tauri.
- [x] Кнопки **Upload** / **Download** через нативный диалог (`tauri-plugin-dialog`);
      скачивание как файлов, так и **папок целиком** (рекурсивно).
- [x] **Индикатор прогресса** передачи (события `sftp://progress`, прогресс-бары).

**Артефакт:** обмен файлами с drag-and-drop, как в MobaXterm.

**Сборка:** оба дистрибутива (macOS + Windows) — зелёная CI-сборка на этом этапе.

---

## ✅ Фаза 4 — Полировка UX

**Цель:** довести до уровня зрелого инструмента.

### Сессии и список серверов
- [x] Несколько одновременных сессий (вкладки терминалов) — **реализовано досрочно**
      как промежуточный этап: имена серверов на вкладках, drag-and-drop переупорядочивание,
      индикатор статуса на каждой вкладке, параллельные сессии (в т.ч. к одному серверу).
- [x] **Сворачивание/разворачивание левой панели** со списком серверов: кнопка-тогл
      в заголовке панели, запоминание состояния и ширины между запусками (localStorage).
- [x] **Регулируемая ширина панелей перетаскиванием границ**: левая панель со списком
      серверов и правая панель SFTP. Захват границы мышью (курсор-ресайз) и
      перетаскивание для изменения ширины каждой из частей; запоминание ширин между
      запусками; минимальная/максимальная ширина (левая 160–560 px, SFTP 240–720 px).
- [x] **Сворачивание левой и правой панелей в тонкую полоску**: у каждой панели своя
      иконка-тогл; клик сворачивает панель в узкую полоску, повторный клик по той же
      иконке — разворачивает. Левая (список серверов) и правая (SFTP) сворачиваются
      независимо, состояние каждой запоминается между запусками.
- [x] Группы/папки серверов, поиск по списку, теги: **иерархические папки**
      (вложенность любой глубины, пустые папки сохраняются), создание папки/подпапки и
      удаление из меню списка, **перетаскивание сервера в папку мышью**, сворачивание
      папки пиктограммой +/−; в форме сервера папка выбирается из списка с поиском
      (datalist); теги-чипы; строка поиска (по alias/host/логину/папке/тегам). Папки
      хранятся в `folders.json`, состояние свёрнутости — в localStorage.
- [x] Переподключение при обрыве: кнопка **Reconnect** в области терминала при
      разрыве/ошибке (переоткрывает сессию с теми же учётными данными); опциональный
      авто-reconnect (тумблер в настройках).
- [x] **Подтверждение закрытия вкладки**: модальное окно «Закрыть вкладку? Сессия
      будет разорвана» с Cancel/Close при закрытии вкладки с активной сессией
      (управляется тумблером в настройках, по умолчанию включено).

### Нижняя строка состояния (как в MobaXterm)
- [x] **Статус-бар внизу окна** для активной вкладки с данными удалённого сервера:
  - **иконка ОС** (Linux-дистрибутив / macOS / *BSD) — определять по `/etc/os-release`
    или `uname`;
  - **hostname** и **пользователь**, под которым подключились (`whoami` / из профиля);
  - **загрузка ЦП** — зелёный мини-график процента + число справа (CPU% считается
    дельтой `/proc/stat` между опросами); пиктограмма-микросхема;
  - **ОЗУ** (использовано/всего — `/proc/meminfo`); пиктограмма-планка памяти;
  - **свободное место на диске** (`df -kP /`); пиктограмма-HDD.
- [x] Сбор метрик — лёгким периодическим one-liner'ом по **отдельному exec-каналу**
      russh (раз в N секунд), чтобы не мешать интерактивному shell. Период опроса — в
      настройках.
- [x] Грейсфул-деградация: если команд нет (ограниченный shell) — показывать прочерки.

### Темы оформления
- [x] Переключение тем терминала и UI; настройка шрифта (семейство, размер, межстрочный
      интервал) — применяется в реальном времени.
- [x] Набор готовых тем (реализовано подмножество):
  - **Современные:** Catppuccin (по умолчанию), Dracula, Nord, Gruvbox Dark,
    Solarized Dark, One Dark, Tokyo Night.
  - **Ретро / тематические:** Fallout Pip-Boy (зелёный фосфор), Amber CRT (янтарный
    монохром), IBM 3270 («зелёный экран»), Commodore C64.
  - _(можно дополнить: light-варианты Gruvbox/Solarized, Monokai, Tomorrow Night,
    Campbell — добавляются одним объектом палитры в `src/lib/themes.ts`.)_
- [x] Пользовательская тема: свои цвета фона/текста/курсора и 16-цветной ANSI-палитры
      (color-пикеры в настройках при выборе темы «Custom»).

> **Анализ распространённых тем терминалов.** Самые популярные «современные» схемы —
> **Solarized** (Dark/Light, де-факто классика), **Dracula**, **Gruvbox**, **Nord**,
> **One Dark** (Atom), **Monokai**, **Tokyo Night**, **Catppuccin**, **Tomorrow Night**;
> из встроенных в эмуляторы — **Campbell** (Windows Terminal), **Tango** (GNOME),
> **Solarized**/**Basic** (iTerm2). Ретро-семейство, востребованное для «эстетики»:
> **зелёный фосфор P1** (Fallout Pip-Boy, классические VT/IBM зелёные экраны),
> **янтарный (amber) монохром**, **IBM 3270/3279**, **Commodore C64**. Технически тема
> в xterm.js — это объект `ITheme` (background, foreground, cursor + 16 ANSI-цветов),
> так что каждую схему достаточно описать набором цветов и хранить в JSON.

### Настройки (кнопка ⚙ Settings)
- [x] **Кнопка «Настройки»** в верхнем меню → отдельная панель. Что настраивается:
  - **Внешний вид:** тема терминала и UI, шрифт (моноширинный), размер шрифта,
    межстрочный интервал.
  - **Курсор:** форма (блок / линия / подчёркивание), мигание.
  - **Терминал:** размер буфера прокрутки (scrollback), сигнал bell (звук/визуальный),
    копирование по выделению, вставка средней кнопкой.
  - **Поведение:** подтверждение закрытия вкладки с активной сессией; авто-reconnect.
    (Стартовое состояние/ширина боковой панели запоминаются автоматически между
    запусками — отдельный layout-store.)
  - **Соединение:** таймаут подключения, интервал keepalive, тип терминала (`TERM`),
    порт по умолчанию.
  - **Статус-бар:** показывать/скрывать нижнюю строку, период опроса метрик.
  - **Безопасность:** политика host key (строгая / TOFU / принимать любой).
  - **Язык интерфейса** (ru/en) — _не реализован (опционально)._
- [x] Хранение настроек: реактивный store на `localStorage` (`src/lib/settings.svelte.ts`).
      _(При желании можно перенести в `tauri-plugin-store`.)_

### Справка и «О программе»
- [x] **Меню Help:** краткая справка по использованию, список горячих клавиш,
      ссылки на технологии (вкладка Help в окне Help & About).
- [x] **Окно About:** название, **версия приложения** (через `@tauri-apps/api/app`
      `getVersion`), лицензия, использованные технологии (Tauri / russh / xterm.js /
      SvelteKit).

### Прочее
- [x] Копирование/вставка: copy-on-select, copy (⌘C / Ctrl+Shift+C), paste
      (⌘V / Ctrl+Shift+V), вставка средней кнопкой. Через `navigator.clipboard`
      (изолировано в `src/lib/clipboard.ts` — легко заменить на
      `tauri-plugin-clipboard-manager`).
- [x] **Проверка отпечатка сервера / `known_hosts`** (важно для безопасности):
      vterm-managed `known_hosts.json` (SHA256-отпечаток на `host:port`); политики
      strict / TOFU-reject / accept-any в настройках. _(Интерактивный prompt с
      отпечатком при изменении ключа — возможное улучшение; сейчас изменённый ключ
      отклоняется.)_

**Артефакт:** удобный повседневный клиент с темами, настройками, справкой и нижним
статус-баром удалённого сервера.

**Сборка:** оба дистрибутива (macOS + Windows) — зелёная CI-сборка на этом этапе.

---

## ✅ Фаза 5 — Тесты и покрытие

**Цель:** построить страховочную сеть из тестов **до** масштабного рефакторинга, с
максимально разумным покрытием и гейтами в CI, чтобы рефакторинг был безопасным.

> **Статус:** реализовано. 23 Rust-юнит-теста + 71 фронтенд-тест (юнит +
> компонентные) проходят; покрытие чистой логики ≥ 90 %, гейты в CI. Полная
> документация — [TESTS.md](TESTS.md). Интеграция с реальным SSH/SFTP (5.3)
> выполняется E2E-набором против контейнера `linuxserver/openssh-server`; E2E и
> часть компонентных тестов «тяжёлых» панелей прогоняются в CI на Linux
> (`tauri-driver` не работает на macOS).

### 5.1 Инструментарий и инфраструктура
- [x] **Rust:** `cargo test`, `cargo-llvm-cov` (покрытие), `tempfile` для файловых
      тестов. Набор совместим с `cargo-nextest`.
- [x] **Фронтенд:** **Vitest** (юнит + компонентные), `@testing-library/svelte`,
      `@vitest/coverage-v8`, jsdom ([vitest.config.ts](vitest.config.ts)).
- [x] **E2E:** **WebdriverIO + `tauri-driver`** ([e2e/](e2e/)) — нативное окно
      (Linux/Windows; на macOS — юнит/компонентные тесты).
- [x] **Тестовый SSH-сервер:** контейнер `linuxserver/openssh-server`
      ([e2e/docker-compose.ssh.yml](e2e/docker-compose.ssh.yml)) + сервис в CI.
- [x] **Линтеры как гейты:** `cargo fmt --check`, `cargo clippy -D warnings`,
      `pnpm check` (svelte-check). _(eslint/prettier пока не подключены к проекту.)_

### 5.2 Rust — юнит-тесты (чистая логика, без сети)
- [x] `lib.rs`: `parse_metrics`, `parse_cpustat` (краевые: пустые поля, мусор),
      `normalize_path`, `reprefixed` (перенос/переименование, поддерево), `uuid_like`.
- [x] `model.rs`: serde round-trip `ServerProfile`/`NewServerProfile`/`AuthMethod`
      (camelCase; `#[serde(default)]` для старых JSON без `group`/`tags`).
- [x] `store.rs`: декодирование профилей/папок/`known_hosts`, битый JSON → дефолты,
      файловый round-trip во временном каталоге.
- [x] `ssh.rs`: `HostKeyPolicy::from_str`, имена событий, `key_is_encrypted` на
      сгенерированных `ssh-keygen` ключах.
- [x] Цель покрытия модулей чистой логики — **≥ 90%**.

### 5.3 Rust — интеграционные тесты (против тестового SSH)
> Выполняются E2E-набором (5.6) против контейнера `linuxserver/openssh-server` —
> он поднимает весь стек (russh → PTY → exec → SFTP) как боевое приложение.
- [x] `connect` по паролю; неверные креды → `AUTH_REJECTED` (через UI re-prompt).
- [x] PTY: shell, ввод/вывод, вывод команды.
- [ ] (расширить) exec/SFTP upload-download, resize, host-key strict/TOFU/accept.

### 5.4 Фронтенд — юнит-тесты (Vitest, мок `@tauri-apps/api`)
- [x] `settings.svelte.ts`: загрузка/дефолты/persist, применение UI-палитры, reset.
- [x] `themes.ts`: целостность палитр (все ключи), `getTheme`, `applyUiPalette`.
- [x] `clipboard.ts`: write/read с моками `navigator.clipboard`/`execCommand`.
- [x] `api.ts`: каждая обёртка вызывает `invoke`/диалог с правильным именем/аргументами.
- [x] **Логика дерева** (вынесена в чистый модуль [tree.ts](src/lib/tree.ts)):
      `buildTreeRows`, эффективные папки, группировка, фильтр поиска,
      `parentOf`/`nameOf`/`groupOf`, валидация drop (`dropAllowed`).
- [x] Форматтеры `StatusBar` ([format.ts](src/lib/format.ts)): `fmtBytes`, проценты,
      выбор иконки ОС.

### 5.5 Фронтенд — компонентные тесты (`@testing-library/svelte`)
- [x] `HelpPanel`: вкладки Help/About, хоткеи, версия из мока, внешние ссылки.
- [x] `StatusBar`: рендер ОС/`user@host`/ресурсов, состояние ошибки.
- [x] Логика дерева серверов/папок — покрыта юнит-тестами `tree.ts` (вынесена из DOM).
- [ ] `SettingsPanel` / `SftpPanel` — проверяются E2E-набором (тяжёлые нативные API).

### 5.6 E2E (happy-path)
- [x] Запуск, добавление сервера, подключение к тестовому SSH, ввод команды, проверка
      вывода ([e2e/specs/app.e2e.js](e2e/specs/app.e2e.js)).
- [ ] (расширить) SFTP upload/download; папки create/rename/move/delete; темы/меню.

### 5.7 CI-пайплайн (GitLab)
- [x] Стадии: `lint` → `test:rust` (+coverage) → `test:web` (+coverage) → `e2e`
      (с сервисом SSH) → `build` → `release`. Кэш cargo/pnpm.
- [x] **Гейт по покрытию** (80% общий, 90% для чистой логики — в
      [vitest.config.ts](vitest.config.ts)); junit + Cobertura для виджета MR.

### 5.8 Подготовка кода к тестируемости (минимальные правки)
- [x] Чистая логика вынесена из `+page.svelte`/`StatusBar.svelte` в `.ts`-модули
      ([tree.ts](src/lib/tree.ts), [format.ts](src/lib/format.ts)) — тестируется без DOM.
- [x] Проставлены `data-testid` на ключевых элементах (`+page.svelte`) для e2e.

**Артефакт:** зелёный CI с прогоном всех видов тестов и отчётом покрытия — безопасная
база для рефакторинга.

---

## ✅ Фаза 6 — Рефакторинг: безопасность, архитектура, оптимизация

**Цель:** на основе тестов Фазы 5 безопасно отрефакторить проект: безопасность,
автономность, архитектура/DRY, производительность, удобство дальнейшей разработки.

> **Статус:** выполнено полностью (6.1–6.5). Все тесты зелёные (26 Rust + 122
> фронтенд), `pnpm check`/clippy/fmt чисты, security-гейты (cargo audit/deny,
> pnpm audit, Semgrep) проходят. Политика безопасности — в [SECURITY.md](SECURITY.md),
> архитектура — в [docs/adr/](docs/adr/), инварианты — в [CONTEXT.md](CONTEXT.md).

### 6.1 Безопасность (SAST и аудит)
- [x] **Rust:** `cargo audit` (RUSTSEC) + `cargo deny` (advisories/licenses/bans/
      sources) — настроены и зелёные; политика в [deny.toml](src-tauri/deny.toml).
- [x] **JS/TS:** `pnpm audit` (чисто — `cookie` обновлён override'ом) + **Semgrep**
      (`p/typescript`/`p/javascript`/`p/secrets`, 0 findings). _(CodeQL — опционально, не делалось.)_
- [x] **Бандл:** `Trivy` (`fs`: vuln/secret/misconfig) добавлен в CI.
- [x] **Tauri-специфика:** строгий **CSP** + `devCsp` в `tauri.conf.json` (раньше был
      `null`); capabilities — наименьшие привилегии (dialog open/save, opener только
      `https://`); нет `dangerous*`. Зафиксировано гейтом
      [tauri-security.guard.test.ts](src/lib/tauri-security.guard.test.ts).
- [x] **Секреты/крипто:** keychain-only, нет логирования секретов, `zeroize` для
      копий в памяти, безопасный host-key дефолт (TOFU-reject).
- [x] Найденное исправлено; проверки добавлены как CI-гейты (стадия `security`).
      **Принятое исключение:** RUSTSEC-2023-0071 (`rsa` Marvin — нет фикса; используйте
      ed25519-ключи), задокументировано в [SECURITY.md](SECURITY.md)/deny.toml.

### 6.2 Автономность и внешние зависимости
- [x] Аудит: приложение **полностью офлайн**, кроме исходящего SSH к серверам
      пользователя.
- [x] Подтверждено отсутствие runtime-обращений в интернет (шрифты `@fontsource`
      встроены, нет CDN/Google Fonts/телеметрии; `openUrl` — только по явному клику) —
      закреплено тестом-гейтом [autonomy.guard.test.ts](src/lib/autonomy.guard.test.ts).
- [x] Сетевой доступ (только SSH) описан в [README](README.md#сеть-и-автономность).
- [x] Воспроизводимость: lock-файлы закоммичены, CI ставит `--frozen-lockfile`.

### 6.3 Архитектура и DRY
- [x] **Декомпозиция `+page.svelte`** (~1500 → ~720 строк): выделены
      [ServerTree.svelte](src/lib/ServerTree.svelte), [TopBar.svelte](src/lib/TopBar.svelte),
      общие [Modal.svelte](src/lib/Modal.svelte), [ConfirmDialog.svelte](src/lib/ConfirmDialog.svelte),
      [Icon.svelte](src/lib/Icon.svelte) (SVG-реестр [icons.ts](src/lib/icons.ts) вместо эмодзи).
- [x] Общая **pointer-drag** логика — в [actions/drag.ts](src/lib/actions/drag.ts)
      (`use:resizableHandle` + чистые хелперы).
- [x] Единый слой ошибок в Rust: [error.rs](src-tauri/src/error.rs) (`AppError`/`thiserror`)
      вместо `String` во всех командах. _(Тосты на фронте — на будущее.)_
- [ ] **Синхронизация типов Rust↔TS** через `ts-rs`/`specta` — отложено (ручное
      зеркало `types.ts` сохранено; serde-round-trip покрыт тестами).
- [x] Состояние: layout/вкладки — runes-сторы
      [stores/layout.svelte.ts](src/lib/stores/layout.svelte.ts) и
      [stores/tabs.svelte.ts](src/lib/stores/tabs.svelte.ts).
- [x] `unwrap()` на ошибках устранён в командном пути (остались только lock-poison).

### 6.4 Производительность
- [x] xterm: **WebGL-рендерер** (`@xterm/addon-webgl`) с graceful-fallback.
- [x] Метрики: пауза опроса при скрытом окне (`document.hidden`) + мгновенный опрос
      при возврате видимости.
- [x] Дебаунс `ResizeObserver`; тяжёлые `$derived` мемоизируются Svelte по зависимостям.
- [ ] Виртуализация длинных списков и ленивая загрузка (Monaco — Фаза 12) — на будущее.

### 6.5 Оптимизация под дальнейшую разработку
- [x] [CONTRIBUTING.md](CONTRIBUTING.md), ADR в [docs/adr/](docs/adr/), документация модулей.
- [x] Чёткие границы фронт/бэк, публичные API команд (`lib.rs`).
- [x] Расширяемость: реестры иконок/тем/шрифтов/настроек как точки расширения.

**Артефакт:** более безопасная, чистая и быстрая кодовая база с зелёными
security-гейтами и архитектурой, готовой к расширению.

---

## ✅ Фаза 7 — Дизайн и UX-ревизия

**Цель:** проанализировать дизайн и довести визуал/UX до продуктового уровня.

> **Статус:** завершена. Визуальный язык закреплён как инвариант в [CONTEXT.md](CONTEXT.md)
> (раздел «Дизайн-система»). Несколько пунктов (брендинг: иконка/сплэш; формальный
> мини-гайдлайн) **оставлены не отмеченными — сделаем позже**. **Феатура 1 — «Единая
> иконография» (v0.7.1)** выполнена:
> все эмодзи/инлайн-SVG вынесены в единый реестр [icons.ts](src/lib/icons.ts) и
> рендерятся через `<Icon>`. SFTP-панель ([SftpPanel.svelte](src/lib/SftpPanel.svelte))
> переведена на тот же набор, что и список серверов: одинаковые иконки папок/файлов
> слева и справа, символьная ссылка получила свою иконку, кнопки download/delete и
> «новая папка»/refresh/сворачивание оформлены как у серверов (карандаш/корзина/
> шевроны вместо крестиков и `«»`). Иконки ОС статус-бара (🍎/🐧/🪟/BSD/unknown) и
> CPU/RAM/disk тоже переехали в реестр. Кнопки (Connect/Add server/Upload/New folder)
> сведены к единой кнопочной системе (primary-green / neutral / иконочные тулбар- и
> строчные варианты), появление строчных действий — без сдвига строки. Сам
> визуальный язык **закреплён как инвариант** в [CONTEXT.md](CONTEXT.md) и не
> меняется без явного запроса. **Феатура 2 — «Тосты/уведомления» (v0.7.2)**:
> добавлены рун-стор тостов и компонент `Toast`; неблокирующие ошибки/статусы
> операций (SFTP, серверный/папочный CRUD) выведены в тосты вместо инлайновых
> баннеров. **Феатура 3 — «Командная палитра ⌘K» (v0.7.3)**: оверлей `CommandPalette`
> с чистой логикой ранжирования `command.ts`; ⌘K даёт быстрый доступ к серверам,
> папкам, действиям и настройкам с клавиатурной навигацией. **Феатура 4 —
> «Доступность a11y» (v0.7.4)**: фокус-менеджмент и семантика диалогов в `Modal`
> (фокус-трап, автофокус, возврат фокуса), роуминг-навигация по вкладкам
> (`nextTabIndex`), ARIA на интерактивных элементах. **Феатура 5 — «Пустые
> состояния и онбординг» (v0.7.5)**: переиспользуемый `EmptyState` для пустого
> списка серверов и отсутствия активной сессии (дружелюбные экраны с CTA и
> подсказкой про ⌘K). **Феатура 6 — «Анимации/переходы» (v0.7.6)**: плавное
> сворачивание панелей (`transition-[width]`, выключается на ресайзе), drag-фидбек
> (затемнение строк, подсветка drop-таргета, `fade` у призраков), скелетоны SFTP
> ([Skeleton.svelte](src/lib/Skeleton.svelte)) и глобальный guard
> `prefers-reduced-motion`. **Феатура 7 — «Светлые темы + предпросмотр + поиск по
> настройкам» (v0.7.7)**: 4 светлые палитры, визуальный пикер тем с образцами,
> превью шрифта, поиск по секциям настроек. **Феатура 8 — «Полировка статус-бара»
> (v0.7.8)**: компактный/расширенный режим, выбор показателей чекбоксами, индикатор
> передач SFTP (общий стор) с разворотом панели по клику. Все гейты зелёные
> (32 Rust + 192 фронтенд, покрытие выше порогов).

- [ ] **Аудит UI** и проработка предложений:
  - [x] **Единая иконография:** эмодзи (📁/📄/🍎/🐧) заменены на согласованный
    line-SVG-набор из единого реестра — вид не зависит от системного рендера эмодзи;
    иконки папок/файлов одинаковы в списке серверов и в SFTP (единая минималистичная
    дизайн-система).
  - [x] Кнопки редактирования/удаления в SFTP приведены к виду серверов (карандаш/
    корзина вместо старых крестиков).
  - [x] Дизайн-система закреплена в [CONTEXT.md](CONTEXT.md) (раздел «Дизайн-система»):
    её нельзя менять при рефакторинге или иных действиях — только по прямому запросу.
  - [x] **Командная палитра** (⌘K, v0.7.3): оверлей [CommandPalette.svelte](src/lib/CommandPalette.svelte)
    с чистой логикой ранжирования [command.ts](src/lib/command.ts) — быстрый доступ к
    серверам (подключение), папкам (добавить сервер), действиям и настройкам;
    клавиатурная навигация ↑/↓/Enter/Esc, глобальный хоткей ⌘K/Ctrl+K.
  - [x] **Тосты/уведомления** (v0.7.2): рун-стор [toasts.svelte.ts](src/lib/stores/toasts.svelte.ts)
    + компонент [Toast.svelte](src/lib/Toast.svelte); ошибки операций SFTP и серверного/
    папочного CRUD выведены в неблокирующие тосты (плюс success-статусы) вместо
    инлайновых баннеров/«проглоченных» исключений.
  - [x] **Доступность (a11y)** (v0.7.4): управление фокусом в модалках
    ([Modal.svelte](src/lib/Modal.svelte) — `role="dialog"`/`aria-modal`/`aria-label`,
    фокус-трап, автофокус, возврат фокуса открывшему), роуминг-навигация по вкладкам
    (↑/↓/←/→/Home/End, чистый `nextTabIndex`), ARIA на интерактивных элементах.
    _(Контраст ретро-тем — отдельной задачей с правкой палитр.)_
  - [x] **Пустые состояния и онбординг** (v0.7.5): переиспользуемый
    [EmptyState.svelte](src/lib/EmptyState.svelte) — дружелюбные экраны для пустого
    списка серверов (единственная CTA «Добавить сервер» — в левой панели) и
    отсутствия активной сессии (в центре — только Connect для выбранного сервера +
    подсказка про ⌘K; заголовок «Сервер «name»»). Добавление сервера — через кнопку
    в тулбаре списка серверов (рядом с «Новая папка»), центральная кнопка добавления
    убрана как дублирующая.
  - [x] **Анимации/переходы** (v0.7.6): плавное сворачивание панелей
    (`transition-[width]`, выключается на время ресайза), drag-фидбек (плавное
    затемнение строк/подсветка drop-таргета, `fade` у drag-призраков), скелетоны SFTP
    ([Skeleton.svelte](src/lib/Skeleton.svelte)); глобальный guard
    `prefers-reduced-motion`. Токены движения и паттерны закреплены в [CONTEXT.md](CONTEXT.md).
  - [x] **Светлые темы + предпросмотр + поиск по настройкам** (v0.7.7): 4 светлые
    палитры (Solarized/GitHub/Catppuccin Latte/Gruvbox Light) в [themes.ts](src/lib/themes.ts);
    визуальный пикер тем с образцами цветов (`themeSwatches`), опции шрифта в своём
    начертании + живой образец, поиск по настройкам (`matchesQuery`, фильтр секций).
  - [ ] **Брендинг:** иконка приложения, сплэш, единый visual language, типографика.
  - [x] **Полировка статус-бара** (v0.7.8–0.7.9): компактный (по умолч.) / расширенный
    режим с тогглом; выбор видимых показателей чекбоксами (свёрнутый под-раздел настроек);
    **индикатор передач SFTP** в баре (общий стор `transfers.svelte.ts`,
    `aggregateTransfers`) с кликом → разворот панели SFTP; **load average** отдельной
    группой с иконкой-спидометром; **сеть** ↑/↓ (бэкенд-дельта `/proc/net/dev`, `fmtRate`).
    Правило в CONTEXT: между любыми группами статус-бара — обязательный разделитель.
    **(v0.7.10)** добавлены показатели: uptime, swap, disk I/O, сетевые соединения,
    залогиненные пользователи (число + список в тултипе), IP, топ-процесс по CPU,
    температура CPU, kernel, время сервера/TZ — каждый со своей иконкой, чекбоксом и
    авто-скрытием при отсутствии данных; бар скроллится по горизонтали при переполнении.
    **(v0.7.11)** значения с фиксированной шириной (бар не «прыгает»), длинные тексты
    усечены с `…` и полным значением в тултипе (top process → топ-3), uptime без «up»,
    плотные пары rx/tx.
  - [x] **Создание сервера/файла из тулбаров** (v0.8.0): кнопка «Добавить сервер»
    убрана из верхней панели ([TopBar.svelte](src/lib/TopBar.svelte)) и перенесена в
    тулбар списка серверов ([ServerTree.svelte](src/lib/ServerTree.svelte)) рядом с
    «Новая папка» — новая иконка `filePlus` (лист с «+», в стиле файла из SFTP). В
    SFTP-панели ([SftpPanel.svelte](src/lib/SftpPanel.svelte)) рядом с «Новая папка»
    добавлена такая же кнопка **создания пустого файла** (бэкенд-команда
    `sftp_create_file` → `sftp::create_file`, обёртка `sftpCreateFile`).
- [ ] Зафиксировать решения в мини-гайдлайне (design tokens уже есть в `@theme`).

**Артефакт:** согласованный визуальный язык и приоритизированный список UX-улучшений.

---

## ✅ Фаза 8 — Документация (промежуточная)

**Цель:** привести пользовательскую и проектную документацию в соответствие с уже
реализованным функционалом и сделать инструкцию доступной прямо из приложения.

> **Промежуточная фаза**, вставлена между «Дизайном» (7) и «Расширением
> мониторинга» (9) / «Умными логами» (10). Не путать с фазой 13 «Актуализация и
> оформление README» — та про финальный «продуктовый» вид (скриншоты/гифки/бейджи)
> перед релизом; эта приводит README/инструкцию в актуальное состояние сейчас.

- [x] **Актуализация README** под Фазы 0–7: статус, раздел «Возможности»
      (иконография, тосты, ⌘K, a11y, пустые состояния, анимации, светлые темы,
      расширенный статус-бар, дефолтный ключ `~/.ssh`, нативная вставка), структура
      каталогов, технологический стек, переименование фаз в плане.
- [x] **Описание продукта** изменено на «Кроссплатформенный инструмент для работы
      системных администраторов, devops, sre» (README + About).
- [x] **Пошаговая установка на Windows** с конкретными командами, включая установку
      системных компонентов (Rust, Node/pnpm, VS C++ Build Tools, WebView2) —
      раздел [README](README.md#установка-на-windows-с-нуля).
- [x] **Инструкция (README) в приложении:** отдельная вкладка **«Инструкция»** в окне
      Help & About ([HelpPanel.svelte](src/lib/HelpPanel.svelte)) и пункт нативного
      меню **Help → Инструкция** ([lib.rs](src-tauri/src/lib.rs),
      событие `menu://manual`), плюс команда в палитре ⌘K. README бандлится
      (`?raw`) и рендерится собственным мини-парсером Markdown
      ([markdown.ts](src/lib/markdown.ts)); ссылки открываются только по клику через
      opener (офлайн-инвариант сохранён).
- [x] **Правило в CONTEXT:** README обновляется наравне с ROADMAP/TESTS/CONTEXT при
      добавлении или фиксе чего-либо ([CONTEXT.md](CONTEXT.md)).
- [x] **Расширенное версионирование:** последняя цифра версии — номер фикса/хотфикса;
      т.к. Cargo/SemVer допускают только 3 числа, схема — `0.<фаза>.<фикс>` (номер
      фичи ведётся в этом ROADMAP), сброс фикса в 0 при смене фазы. Описано в
      [CONTEXT.md](CONTEXT.md). Версия поднята до **0.8.0**.
- [x] **Контакты автора** в About: email `bt@vcore.su`, Telegram `@BorisToboltsov`.

**Артефакт:** актуальные README и проектные доки; инструкция доступна из меню и из
окна Help; зафиксированы правила обновления доков и версионирования.

**Сборка:** оба дистрибутива (macOS + Windows) — зелёная CI-сборка на этом этапе.

---

## ✅ Фаза 9 — Расширение мониторинга (промежуточная)

**Цель:** превратить статус-бар из набора цифр в полноценный инструмент
мониторинга: отдельная страница с подробными метриками и графиками, пороги с
цветовой индикацией, полное отключение бара и метрики, помогающие избегать
исчерпания ресурсов (дескрипторы, inodes).

> **Промежуточная фаза**, вставлена между «Документацией» (8) и «Умными логами»
> (теперь 10). Версия поднята до **0.9.0**.

- [x] **Страница подробного мониторинга** ([MonitoringOverlay.svelte](src/lib/MonitoringOverlay.svelte)):
      полноэкранный оверлей на базе `Modal`, открывается кнопкой в статус-баре
      (иконка `barChart`, после тоггла компакт/расширенный), командой ⌘K
      «Мониторинг сервера» **и** нативным меню (**Мониторинг**, событие
      `menu://monitoring`) — точки входа сохраняются при выключенном статус-баре.
      **Данные тянутся только пока страница открыта**: петля опроса стартует на
      открытии и гасится на закрытии; графики (CPU/load/RAM/swap) копятся с момента
      открытия. Карточки: ЦП (история + **загрузка по ядрам**, load/ядро, PSI,
      ctx-switch/прерывания, процессы run/blk, температура, топ CPU), Память
      (used/avail/cache/buffers/free, swap, PSI, топ MEM), Файловые системы (все
      разделы со столбиками места + **inodes**, **системные дескрипторы**
      `/proc/sys/fs/file-nr`, `ulimit -n`, disk I/O, PSI), Сеть (rx/tx, соединения,
      **состояния TCP**, пользователи), Load average, Обновления.
- [x] **Пороги (средний/предельный) с цветовой индикацией** ([thresholds.ts](src/lib/thresholds.ts)):
      на все числовые метрики (ЦП, ОЗУ, swap, диск, inodes, FD, load, темп.) — при
      превышении **среднего** текст подсвечивается **жёлтым** (`text-warn`), при
      превышении **предельного** — **красным** (`text-danger`); и в статус-баре, и
      на странице. Настраиваются в свёрнутом под-разделе настроек
      ([SettingsPanel.svelte](src/lib/SettingsPanel.svelte), `thresholds-toggle`),
      хранятся в `settings.statusBarThresholds`. Добавлен токен `warn` (янтарный) в
      [app.css](src/app.css) и `UiPalette`.
- [x] **Полная статистика по разделам** — только на странице: каждый смонтированный
      ФС с местом и inodes, без захламления статус-бара.
- [x] **Полное отключение статус-бара** ([settings](src/lib/settings.svelte.ts),
      `showStatusBar`): бар размонтируется и **перестаёт опрашивать сервер**; кнопка
      открытия страницы остаётся доступной через ⌘K и нативное меню.
- [x] **Pending updates — лениво** ([fetch_pending_updates](src-tauri/src/lib.rs)):
      тяжёлый distro-aware подсчёт обновлений (apt/dnf/yum/pacman/zypper/apk) +
      «требуется перезагрузка» вынесен в **отдельную команду**, дёргается **после**
      первого рендера страницы (скелетон), не входит в статус-бар и обычный опрос.
- [x] **Дескрипторы файлов и inodes** (борьба с «невозможно создать файл»):
      системные FD (`/proc/sys/fs/file-nr` used/max), `ulimit -n`, и **inodes на
      каждый раздел** (`df -i`) с порогами.
- [x] **Доп. метрики:** ЦП по ядрам, детализация ОЗУ, PSI (`/proc/pressure`),
      состояния TCP (`ss`), ctx-switch/прерывания (rate), процессы running/blocked,
      топ-процессы по памяти. Бэкенд — отдельная команда
      [fetch_metrics_detail](src-tauri/src/lib.rs) (`DETAIL_SCRIPT`), пер-сессионные
      сэмплы для дельт (per-core, ctxt/intr).

**Артефакт:** страница мониторинга с графиками и порогами; статус-бар с цветовой
индикацией и полным отключением; новые ресурсные метрики. Все пять гейтов зелёные
(49 Rust + 252 фронтенд, покрытие выше порогов).

**Сборка:** оба дистрибутива (macOS + Windows) — зелёная CI-сборка на этом этапе.

---

## ✅ Интернационализация (i18n) — переключение языка (v0.9.1)

**Цель:** убрать «где-то русский, где-то английский» — весь интерфейс на **двух**
языках (English / Русский) с переключателем в настройках и **расширяемой** на
будущие языки архитектурой. Доменные/технические термины (`CPU`, `RAM`,
`Load average`, `Disk I/O`, `IP`, `SFTP`, `SSH`, `keychain`, `passphrase` и т.п.)
намеренно **не переводятся**.

> **Промежуточная правка внутри Фазы 9** (патч-версия): фич-набор Фазы 9 не
> меняется, добавлен слой локализации поверх всего UI. Версия **0.9.1**.

- [x] **Ядро i18n** ([src/lib/i18n/](src/lib/i18n/)): реестр языков `LOCALES`
      ([locales.ts](src/lib/i18n/locales.ts)) как единственная точка расширения,
      словари ([messages.ts](src/lib/i18n/messages.ts)) с `en` как каноном —
      пропущенный ключ в любом языке **не компилируется** (`Record<MessageKey,string>`),
      чистые `resolve`/`interpolate` ([translate.ts](src/lib/i18n/translate.ts),
      плейсхолдеры `{name}`, фолбэк на `DEFAULT_LOCALE` → ключ), реактивный
      `t()`/`setLocale`/`availableLocales` ([index.ts](src/lib/i18n/index.ts)),
      читающий `settings.language` (руна) — UI перерисовывается при смене языка сам.
- [x] **Переключатель языка в настройках** ([SettingsPanel.svelte](src/lib/SettingsPanel.svelte),
      раздел «Язык», `language-select`) + поле `language` в
      [settings.svelte.ts](src/lib/settings.svelte.ts) (дефолт **en**, персист в
      `localStorage`, валидация через `isLocale` при загрузке/импорте бэкапа).
- [x] **Полный перевод UI**: весь видимый текст (надписи, кнопки, заголовки,
      плейсхолдеры, `title`/`aria-label`, тосты, диалоги, сообщения) переведён через
      `t()` во всех компонентах (`+page`, `ServerTree`, `SftpPanel`, `StatusBar`,
      `MonitoringOverlay`, `SettingsPanel`, `HelpPanel`, `CommandPalette`,
      `ConfirmDialog`, `Toast`). Статус вкладки хранится канонически по-английски
      (логика `.startsWith`), локализуется только при выводе (`localizedStatus`).
- [x] **Двуязычный поиск по настройкам** (`SECTIONS.keywords` — EN + RU термины),
      работает независимо от выбранного языка.
- [x] **Локализация нативного меню**: метки строятся из тех же словарей и пушатся в
      бэкенд командой `set_menu_language` ([lib.rs](src-tauri/src/lib.rs)); меню
      пересобирается на старте и при смене языка (`$effect` в
      [+page.svelte](src/routes/+page.svelte)), id пунктов стабильны.
- [x] **Тесты** ([i18n.test.ts](src/lib/i18n/i18n.test.ts)): полнота словарей,
      отсутствие лишних ключей, неизменность технических терминов, интерполяция,
      фолбэки, реактивность `t()`; обновлены строковые ассерты компонентных тестов на
      дефолтный (английский) язык; live-переключение языка в `SettingsPanel.test`.

**Артефакт:** единый язык интерфейса с переключателем; расширение на новый язык =
запись в `LOCALES` + словарь (компилятор требует покрыть все ключи). Правило
«весь текст на всех языках» закреплено в [CONTEXT.md](CONTEXT.md). Гейты зелёные
(272 фронтенд-теста, покрытие выше порогов).

---

## ✅ Фаза 10 — Умная работа с логами и текстом

**Цель:** сделать чтение логов удобнее стандартного `tail -f` — DevOps читает логи
постоянно.

> **Версия поднята до 0.10.0** (новая фаза). Делается по одной задаче с полным
> прогоном гейтов на каждой. **Новый раздел настроек «Логи и текст»** с
> **мастер-чекбоксом** (`settings.smartLogs.enabled`) полностью отключает все
> доработки фазы — терминал ведёт себя как раньше (плюс пер-фичевые тумблеры).

- [x] **Умная подсветка (Regex Highlighting):** правила настраиваются прямо в UI
      (раздел настроек «Логи и текст»): имя, regex, цвет (палитра темы), вкл/выкл,
      учёт регистра, **стили** (жирный / фон / **подсветка всей строки** в стиле
      ccze/lnav) и **переупорядочивание** (↑/↓ — приоритет при пересечении). Дефолты из
      коробки — `ERROR/FATAL` (красный, жирный), `WARNING` (жёлтый), `SUCCESS/OK`
      (зелёный), IP-адреса (синий). Подсветка — вставка ANSI-SGR в поток
      до `term.write` (рендерится **цветами активной темы**), **только в обычном
      буфере** (full-screen TUI вроде vim/htop не трогаются), не ломает существующие
      escape-последовательности. **URL — кликабельные** (`@xterm/addon-web-links`,
      открытие http(s) во внешнем браузере через `tauri-plugin-opener` по клику —
      офлайн-инвариант соблюдён). Чистая логика (`compileRules`/`applyHighlight`/
      `colorToSgr`) в [highlight.ts](src/lib/highlight.ts), правила —
      `settings.highlightRules`. Включается тумблером `settings.smartLogs.highlight`.
- [x] **Структурный просмотр логов (мультиформатный):** тумблер **raw↔structured** в
      области терминала (иконка `table`, в углу, при включённой фиче). В structured-режиме
      строки распознаются на лету с **авто-определением формата построчно**: **JSON**
      (NDJSON), **logfmt** (`key=value`), **syslog** (RFC 3164/5424), **nginx/apache**
      access-логи и **dmesg** (ядро) — всё в единую **таблицу** (Время / Уровень /
      Сообщение) с цветом уровня (`error`→красный, `warn`→жёлтый). Поверх базовых трёх
      колонок — **динамические колонки по полям** (пикер: union ключей всех записей;
      именно это даёт ценность сверх `tail`), **фильтр-чипы по уровню**
      (error/warn/info/debug/other) **+** текстовый фильтр (включая raw), **автоскролл**
      в режиме `tail -f`, **детальный разворот строки** (pretty-JSON/исходная строка +
      **копирование**), **нормализация времени** (epoch/ISO/syslog → локальное `YYYY-MM-DD
      HH:MM:SS`, исходное — в тултипе) и **кнопка «Clear» с подтверждением** (ставит
      «водяной знак», чтобы повторный заход в structured не перечитывал очищенный вывод).
      При включении
      посев из scrollback (склейка перенесённых `isWrapped` строк) + живой разбор; парсинг
      **только** пока вид открыт (нулевой оверхед иначе), буфер ограничен (`MAX_JSON_ENTRIES`).
      Имена полей — **регистронезависимо** + алиасы systemd-journal (`MESSAGE`/`PRIORITY`/
      `SYSLOG_TIMESTAMP`, числовой `PRIORITY`→уровень: `journalctl -o json` из коробки).
      Колонки **изменяются по ширине мышью** — drag-ручка на правом краю заголовка
      (`table-layout: fixed` + `<colgroup>`; общий экшен `resizableHandle` из
      [actions/drag.ts](src/lib/actions/drag.ts); при переполнении — горизонтальный скролл).
      Чистая логика (`parseLogLine`/`parseLogfmt`/`parseSyslog`/`parseNginx`/`parseDmesg`/
      `toLogEntry`/`extractFields`/`levelCategory`/`levelClass`/`availableFields`/`fieldValue`/
      `normalizeTime`/`filterEntries`/`applyFilters`/`colWidth`/`resizedWidth`) в
      [jsonlog.ts](src/lib/jsonlog.ts),
      таблица — [JsonLogView.svelte](src/lib/JsonLogView.svelte). Включается тумблером
      `settings.smartLogs.jsonView`.
- [x] **Поиск по всему буферу терминала** (`@xterm/addon-search`): не только по экрану,
      а по десяткам тысяч строк буфера, с копированием найденного фрагмента вместе с
      контекстом (±5 строк). Оверлей поиска (**Cmd+F** / **Ctrl+Shift+F**) в
      [Terminal.svelte](src/lib/Terminal.svelte): поле + счётчик совпадений + тумблеры
      «учитывать регистр / слово целиком / regex» + навигация (Enter / Shift+Enter,
      кнопки ↑/↓) + кнопка «копировать с контекстом» (±5 строк). Опции регистр/слово/regex
      **запоминаются** (`settings.searchOptions`, персист + общие на вкладки); невалидное
      regex показывает подсказку «Неверное regex» (а не молчит); счётчик — `aria-live`.
      Подсветка совпадений — цветами активной темы (`selectionBackground`; активное — бордер
      в акцентном цвете темы, плавно «нарастает» при переходе); терминал создаётся с
      `allowProposedApi: true` (decoration-API xterm 6 ещё «proposed»). Чистая логика
      (`buildMatcher`/`findMatchRows`/`contextSnippet`/`matchCountLabel`) в
      [search.ts](src/lib/search.ts). Включается тумблером `settings.smartLogs.search`.

**Артефакт:** терминал с подсветкой по правилам (стили + whole-line + переупорядочивание),
мультиформатным структурным просмотром логов и полнобуферным поиском — **готово полностью**.
Все пять гейтов зелёные (51 Rust + 366 фронтенд, покрытие выше порогов).

---

## ✅ Фаза 11 — Аудит и запись сессий (Session Recording)

**Цель:** на проде критично понимать, «кто и что сломал».

> **Версия 0.11.0.** **Статус: завершена.** Сделано по задачам: движок записи (пер-сессия,
> маскирование, метаданные, пауза для экономии диска), библиотека (поиск/мультисортировка/
> правка), ИИ-экспорт (ранбук + метаданные), встроенный плеер, автозапись прод-серверов.

- [x] **Запись сессий (asciicast v2):** кнопка **REC** (полоса вкладок) — **пер-сессия**,
      красный индикатор на вкладке и команды палитры (старт/стоп, библиотека). Пишем
      **stdin + stdout** с таймингами в `asciicast` v2 **на бэкенде** (`Recorder` внутри
      `SshSession`/`LocalPty`, кормится из ридера и `write_input`), файлы — в
      `data_dir/recordings/*.cast`. **Режим записи** (Settings → «Запись сессий»):
      полный с таймингами (как видео) / полный без таймингов / **только команды** —
      `RecordMode`. У нетаймингованных режимов **вывод появляется сразу целиком** (шаг 0, не
      построчно) — тайминг несёт **только набор**, и в обоих режимах с **одним шагом
      `TYPING_STEP`** (посимвольное эхо в «только командах», пер-keystroke ввод в `fullNoTiming`).
      Поэтому **оба untimed-режима играют полностью одинаково** (та же скорость печати, вывод
      сразу) и **на одной замедленной шкале скоростей** — обе записи помечены в заголовке флагом
      **`timed:false`**. В режиме «только команды» эхо при наборе подавляется, а строка
      собирается **построчным редактором с курсором**: вставка символа **по курсору**
      (правка середины строки после ←/→ работает, а не дописывается в конец),
      backspace/Delete, Home/End (Ctrl+A/E), Ctrl+U/Ctrl+W, стрелки и прочие escape —
      интерпретируются мини-парсером; **правки оболочки — история (↑/↓) и tab-completion —
      восстанавливаются из её вывода** (флаг `pending_edit` + `sync_pending`: полная перерисовка →
      видимая строка минус приглашение, простое дополнение → дописка по курсору), так что
      рекол/дополнение-и-правка пишутся точно; на Enter — чистая команда + посимвольное эхо +
      вывод; **пустой Enter игнорируется** (echo пустой строки и повторное приглашение
      подавляются — «долбёжка» Enter не плодит пустых строк). **Затравка приглашением:** приглашение
      перед первой командой печатается до старта REC, поэтому фронт читает текущую строку
      терминала (`currentPromptLine`) и передаёт в `start_recording`, который пишет её первым
      `o`-событием — так первая команда **во всех режимах** идёт с приглашением.
      **Приватность:** ввод после промптов `password:` **маскируется** (эвристика, маркер
      вместо секрета; SSH-пароль в поток не попадает). Команды
      `start/stop/list/delete/read/export_recording`
      ([recording.rs](src-tauri/src/recording.rs), чистые хелперы под тестами).
- [x] **Сохранение/редактирование имени и описания:** после стопа открывается
      [RecordingSaveDialog.svelte](src/lib/RecordingSaveDialog.svelte) — **заголовок** +
      **краткое описание** и две кнопки **Удалить**/**Сохранить**; тот же диалог открывается
      **карандашом** в строке библиотеки (режим **Отмена/Сохранить**) для редактирования.
      Сохранение пишет мету в заголовок asciicast через `set_recording_meta` (кастомные поля
      `description` и `server`, чистая `with_updated_meta`); имя сервера (`server`) пишется при
      старте и **переживает переименование заголовка**.
- [x] **Библиотека записей** ([RecordingsPanel.svelte](src/lib/RecordingsPanel.svelte)):
      список (**заголовок + описание + сервер** с иконкой, дата/размер), **поиск** (по серверу /
      описанию / имени файла / ISO-дате), **редактирование** (карандаш) и
      **многоуровневая сортировка** (клик по «Дата/Сервер/Размер» циклит asc→desc→выкл,
      несколько параметров образуют цепочку приоритетов с бейджем порядка), удаление
      **с подтверждением** (`ConfirmDialog`),
      воспроизведение и **экспорт** в 4 формата (выбор формата — **`Modal` поверх библиотеки**,
      чтобы не клиппило в узком окне): **Markdown-«ранбук»**
      (шапка-метаданные → нумерованные команды в code-span + вывод в `` ```text ``; лучший для ИИ —
      `extractMarkdown`), **только команды** (`extractCommands`),
      **транскрипт** (чистый текст — `extractTranscript`) и **сырой `.cast`**. Сегментация
      команд режимо-зависимая (`inputsArePerLine`): в `commands`-режиме `i` — цельные команды
      без `\r` (раньше Markdown давал **пустой файл** — починено). **Во всех экспортах — блок
      метаданных сессии** (`sessionMetaPairs`/`metadataComment`): сервер, host, ip, user, OS,
      ядро, старт/стоп/длительность, режим, версия. Поиск/
      сортировка/извлечение — чистые функции в [recording.ts](src/lib/recording.ts).
      Офлайн-инвариант цел: ИИ-вызова в приложении нет, только экспорт наружу.
- [x] **Метаданные сессии в записи:** при старте REC фронт собирает окружение (для SSH — один
      опрос `fetchMetrics`: hostname/ip/user/OS/ядро; + host:port из профиля, версия приложения)
      и кладёт в заголовок asciicast (`vterm`-объект; `recordMode`/`startedAt` — бэкенд); на стопе
      бэкенд дописывает `endedAt` (`with_ended_at`). Используется в экспортах и доступно для
      дальнейшего анализа.
- [x] **Пауза записи для экономии диска:** запись **не пишет фоновые потоки**, когда вкладка не
      смотрится. Бэкенд: флаг `paused` у `Recorder` (команда `set_recording_paused`) отбрасывает
      `output`; паузная длительность вычитается из timed-таймстемпов (без дыр в плеере); **input
      авто-возобновляет**. Фронт (`$effect`): запущена только **активная** запись, фоновые — на
      паузе; **переключение** вкладки паузит/возобновляет; **простой** активной вкладки дольше
      `settings.recordIdlePauseSecs` (по умолчанию 20с, настраивается; `0` = выкл) ставит на
      паузу, активность (`onactivity`) возобновляет.
- [x] **Встроенный плеер** ([RecordingPlayer.svelte](src/lib/RecordingPlayer.svelte)):
      открыть запись из библиотеки и «проиграть» в read-only xterm с **play/pause**,
      **перемоткой** (scrub-слайдер) и **скоростью**; время `M:SS / M:SS`.
      Перемотка = reset + повтор вывода до точки (`outputUpTo`), воспроизведение —
      инкрементально по rAF. **Шкала скоростей зависит от тайминга** (`playbackSpeeds`):
      реально-таймингованные — `0.5/1/2/4×` (старт `1×`), синтетические (`timed:false`) —
      **вся шкала на ступень медленнее** `0.25/0.5/1/2×` (старт `0.5×`), чтобы «печатание»
      команд читалось спокойнее. Чистые тайминги (`outputUpTo`/`formatTime`/`castDuration`/
      `playbackSpeeds`) в [recording.ts](src/lib/recording.ts). **Прокрутка — у самого
      xterm** (`scrollback` + подгонка числа строк под вьюпорт через `FitAddon`, `cols`
      записи сохраняются): вьюпорт следит за низом вывода и прокручивается колесом/полосой
      назад, внешней полосы нет. Открывается внутри той же модалки (список↔плеер),
      `i`-события не рендерятся.
- [x] **Автозапись для прод-серверов:** per-server флаг **`autoRecord`** (чекбокс «Автозапись
      сессий» в форме сервера, `model.rs`/`types.ts`). Когда SSH-сессия к такому серверу
      переходит в `connected`, запись **стартует автоматически** (`maybeAutoRecord` в
      [+page.svelte](src/routes/+page.svelte), общий путь старта с ручным REC, режим/маскирование
      из настроек), тост `recordings.autoStarted`. На закрытии сессии запись **финализируется**
      (`finalizeRecordingOnClose` → `stop_recording` ставит `endedAt`), без диалога сохранения
      (он только для ручного стопа); реконнект → новая запись.

**Артефакт:** запись и воспроизведение сессий. **Сделано (фаза завершена ✅):** движок записи
(пер-сессия, маскирование, метаданные, **пауза для экономии диска**), библиотека (поиск/
мультисортировка/правка), ИИ-экспорт (ранбук + метаданные), встроенный плеер, **автозапись
прод-серверов**.

### Доработки в линии 0.11.x (после завершения фазы)

- [x] **0.11.1** — табличный structured-вид логов: **колонки с изменяемой шириной**
      (drag-ручка на правом краю заголовка, `table-fixed` + `<colgroup>`, экшен
      `resizableHandle`); чистые `colWidth`/`resizedWidth` в [jsonlog.ts](src/lib/jsonlog.ts).
- [x] **0.11.2** — **окно ожидания подключения** ([ConnectingOverlay.svelte](src/lib/ConnectingOverlay.svelte)):
      оверлей поверх терминала на время connect — орбита-комета + `alias` + `user@host:port` +
      **чек-лист реальных фаз** (`Соединение → Аутентификация → Сессия`). Фазы приходят
      **реальными** событиями нового канала `term://phase/{id}` (бэкенд эмитит их по ходу
      стадий `connect` в [ssh.rs](src-tauri/src/ssh.rs)); чистый маппинг —
      [connphase.ts](src/lib/connphase.ts) (`phaseSteps`).
- [x] **0.11.3** — **экран ошибки/обрыва в стиле окна подключения** (тот же
      `ConnectingOverlay`, проп `failed`): иконка сервера с красным крестом, чек-лист
      **замирает на упавшей фазе** (видно, где оборвалось), красная деталь от сервера и
      кнопка Переподключиться (раньше тонкая плашка перекрывала текст кнопкой). **Провал
      аутентификации** больше не закрывает вкладку автоматически — остаётся оверлей
      «Аутентификация ✗», ввод секрета заново по кнопке (`reauth`). Маппинг статуса —
      `sshErrorView` в [+page.svelte](src/routes/+page.svelte).
- [x] **0.11.4** — фикс «прыжка»/рассинхрона при разворачивании правой SFTP-панели:
      контент заякорен к неподвижному правому краю **через `position:absolute`**
      (`absolute inset-y-0 right-0`, `width: {width}px`) внутри `relative` +
      `overflow-hidden` корня — у докнутой справа панели при разворачивании движется
      **левая** граница, поэтому контейнер просто **раскрывает** неподвижный контент в
      такт с границей. Абсолютный потомок надёжно отсекается в WebKit каждый кадр (а
      oversize-статичный флекс-ребёнок c `self-end` «мелькал» за левой границей над
      терминалом). Кнопка Connect больше не уходит за центральное окно.
      [SftpPanel.svelte](src/lib/SftpPanel.svelte).
- [x] **0.11.5** — переключатель raw↔structured переделан из плавающей угловой
      иконки (перекрывала приглашение) в **сегмент «Raw | Table»**
      ([ViewModeToggle.svelte](src/lib/ViewModeToggle.svelte)): в raw-режиме плавает
      справа-сверху (оверлей поиска стекается **под** ним), в table-режиме — крайний
      правый элемент тулбара таблицы (убран костыль `pl-10`). Состояние `structured`
      осталось в Terminal. [Terminal.svelte](src/lib/Terminal.svelte),
      [JsonLogView.svelte](src/lib/JsonLogView.svelte).

---

## 🟦 Фаза 12 — Продвинутый SFTP и работа с конфигами

**Цель:** быстро править конфиги на сервере, не только качать файлы.

> **Решения (2026-06-25):** редактор — **CodeMirror 6** (а не Monaco: легче, дружит
> со строгим CSP и офлайн-инвариантом, встроенный diff/merge), полноценный
> **workspace с под-вкладками** под основным таб-баром, разбивка **по под-фазам
> 12.1–12.6**. Diff перед сохранением — **по настройке**; sudo-запись и `.bak` —
> отложены в 12.6.

### ✅ 12.1 — Backend: чтение/запись текста + безопасность прода

- [x] `sftp_read_text(path)` → `{content, eol, size, mode, mtime, sha256, readOnly}`;
      гарды: размер (> 2 МБ) и бинарь (NUL-байт / не-UTF-8) → отказ «качай как файл».
      Контент нормализуется в LF, исходный стиль переносов несётся в `eol`.
- [x] `sftp_write_text(path, content, eol, expectedSha256?)`: **атомарно** (sibling
      temp + rename, не обрезающая запись), **сохранение прав** (mode) оригинала,
      **конфликт-детект** по sha256 → `AppError::FileChangedOnServer` (маркер
      `file-changed`), восстановление исходных EOL на запись.
- [x] Зависимость `sha2` (RustCrypto, без C); чистые хелперы `looks_binary`/
      `detect_eol`/`apply_eol`/`is_read_only`/`sha256_hex`/`temp_sibling` + тесты.
- [x] Фронт-контракт: `TextFile`/`WriteResult` в [types.ts](src/lib/types.ts),
      `sftpReadText`/`sftpWriteText`/`isFileChangedError` в [api.ts](src/lib/api.ts).

### ✅ 12.2 — Workspace с под-вкладками + редактор (CodeMirror)

- [x] Стор [workspaces.svelte.ts](src/lib/stores/workspaces.svelte.ts) (под-вкладки
      `[Терминал]` + редакторы, `active`/`dirty`; чистые `isDirty`/`hasUnsaved`/
      `nextActiveAfterClose`); строка под-вкладок под основным таб-баром (на каждое
      соединение, терминал всегда смонтирован).
- [x] [EditorTab.svelte](src/lib/EditorTab.svelte) на **CodeMirror 6** (бандл локально,
      без CDN); тема редактора — из токенов активной темы ([cmtheme.ts](src/lib/cmtheme.ts),
      `editorTheme`/`isDark`, live-смена при переключении темы). Подсветка по расширению/
      имени: чистый [editorlang.ts](src/lib/editorlang.ts) (yaml/json/markdown/shell/toml/
      ini + plain-fallback; tf/dotfiles/Dockerfile). Cmd/Ctrl+C/X/V через нативный буфер
      (CodeMirror — contenteditable, не input → нужен свой биндинг, как у xterm).
- [x] **Широкий набор языков** (0.12.2–0.12.3): официальные Lezer-парсеры Python /
      JavaScript / TypeScript(JSX/TSX) / Java; всё остальное — из `@codemirror/legacy-modes`
      (уже в дереве, без новых установок): Shell·bash / PowerShell, Dockerfile, Go, Rust,
      Ruby, C·C++·C#, SQL, Lua, Perl, **HTML, CSS/SCSS/Less, XML/SVG**, **nginx, CMake, diff,
      HTTP, Protobuf, Puppet**, Groovy/Gradle, Scala, Kotlin, Dart, Swift, Clojure, Haskell,
      Erlang, Elm, Crystal, R, Julia, CoffeeScript, Visual Basic, Scheme, Common Lisp, OCaml,
      F#, Tcl, D, Verilog, VHDL, Pascal, Fortran, COBOL. Имена/расширения (вкл.
      `Dockerfile`/`Containerfile`/`Gemfile`/`Vagrantfile`/`nginx.conf`/`CMakeLists.txt`/
      `build.gradle`) — в [editorlang.ts](src/lib/editorlang.ts).
- [x] Клик/карандаш по редактируемому файлу в SFTP → открыть редактор (read-text из 12.1;
      бинарь/большой файл → тост, без вкладки); dirty-«●» на под-вкладке; **базовое
      сохранение** (Cmd/Ctrl+S → `sftp_write_text`, тосты, `file-changed` → тост);
      **guard на закрытие** несохранённого (ConfirmDialog) + закрытие воркспейса с вкладкой.

> Diff перед сохранением, явный UI разрешения конфликта `file-changed`, аудит-связка и
> офлайн-линт — в 12.3 (поверх базового сохранения).

### ⬜ 12.3 — Сохранение + Diff (по настройке)

- [ ] `Ctrl+S` → `sftp_write_text`; **diff перед сохранением** под настройкой
      `settings.sftpEditor.diffBeforeSave` (CM MergeView в `Modal`); обработка
      `file-changed` (передоткрыть/перезаписать); аудит-связка с записью сессии;
      офлайн-линт YAML/JSON/TOML.

### ⬜ 12.4 — Markdown-превью + «Открыть с помощью vterm»

- [ ] Тумблер «Код ⇄ Превью» (markdown.ts), `.md` сразу в превью; OS-ассоциации +
      single-instance + разбор argv → локальный терминал + редактор-вкладка.

### ⬜ 12.5 — Синхронизация директорий (Sync folder)

- [ ] `sftp_hash_tree` (хэши через `find … sha256sum` по SSH); сравнение локальной и
      удалённой папки; dry-run превью, направление push/pull/bi, exclude-паттерны,
      докачать только изменённое.

### ⬜ 12.6 — Доводка + безопасность записи

- [ ] sudo-запись (`sudo tee`/temp+`sudo mv`) + опциональный `.bak`; snippets/шаблоны;
      поиск по содержимому (grep по SSH); иконки типов файлов; команды палитры.

**Артефакт:** правка конфигов с подсветкой, diff, превью и синхронизацией папок.

---

## ⬜ Фаза 13 — Актуализация и оформление README (продуктовый вид)

**Цель:** довести README до «продуктового» вида перед релизом.

> Базовая актуализация под Фазы 0–7 уже выполнена в промежуточной **Фазе 8
> (Документация)**. Эта фаза — финальная косметика к релизу.

- [x] Актуализация под Фазы 0–7 и чёткая структура (возможности, установка,
      использование, архитектура, troubleshooting) — сделано в Фазе 8.
- [ ] Освежить под Фазы 9–11 (логи, запись сессий, редактор конфигов).
- [ ] Скриншоты/гифки ключевых сценариев; бейджи (CI, покрытие, версия, лицензия).
- [ ] `CONTRIBUTING.md`, раздел безопасности/автономности (из Фазы 6), лицензия.

**Артефакт:** аккуратный, актуальный README продуктового уровня.

---

## ⬜ Фаза 14 — Сборка и CI/CD (релиз)

**Цель:** релизные, подписанные дистрибутивы под обе ОС.

> Базовая сборка на macOS- и Windows-раннерах уже настроена в Фазе 0
> ([.gitlab-ci.yml](.gitlab-ci.yml)). Эта фаза доводит её до релизного качества.

- [x] GitLab CI: задачи `build:macos` + `build:windows` (сделано в Фазе 0).
- [x] Сборка `pnpm tauri build`: `.dmg` (macOS) и `.msi`/NSIS (Windows) (Фаза 0).
- [ ] **Подпись кода:** Apple Developer ID + нотаризация (macOS), code-signing
      сертификат (Windows) — иначе Gatekeeper/SmartScreen блокируют запуск.
      Сертификаты хранить в CI/CD-переменных GitLab (masked + protected).
- [ ] (Опционально) автообновление через `tauri-plugin-updater`.
- [ ] Публикация в GitLab Releases по тегу `v*` (базовый release-job уже есть).

**Артефакт:** подписанные, готовые к публикации дистрибутивы для macOS и Windows из CI.

---

## Заметки по архитектурным решениям

- **`portable-pty` — для локального терминала.** SSH-вкладки используют удалённый
  PTY канала `russh` напрямую; для **локальной shell-вкладки** («+» в списке
  вкладок) добавлен `portable-pty` (forkpty на Unix, ConPTY на Windows) —
  [pty.rs](src-tauri/src/pty.rs). Локальный PTY переиспользует те же события
  `term://out|closed/{id}` и команды `write_to_terminal`/`resize_pty`/`disconnect`,
  что и SSH, поэтому фронтенд-виджет терминала общий.
- **`russh-keys`** в свежих версиях влит в `russh` — проверить актуальность на момент
  начала Фазы 2.
- **`keyring` vs собственное шифрование** — основным выбран `keyring` (системное
  хранилище). `chacha20poly1305` под мастер-пароль — опция для портативного файла.
- **Хранение профилей** — простой JSON-файл на стороне Rust (`serde` + `directories`),
  без `tauri-plugin-store` (это фронтовое хранилище настроек, дублирование не нужно).
- **Tailwind v4** вместо 3.x из исходного плана — интеграция одним плагином Vite,
  без `tailwind.config.js`/PostCSS.
