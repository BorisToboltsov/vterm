// Registers @testing-library/jest-dom's custom matchers (toBeInTheDocument,
// etc.) on Vitest's `expect` for the type-checker (svelte-check / tsc).
/// <reference types="@testing-library/jest-dom/vitest" />
