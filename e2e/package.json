{
  "name": "vterm-e2e",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for vterm (WebdriverIO + tauri-driver). Linux/Windows only — tauri-driver does not support macOS.",
  "scripts": {
    "test:e2e": "wdio run ./wdio.conf.js"
  },
  "devDependencies": {
    "@wdio/cli": "^9.0.0",
    "@wdio/local-runner": "^9.0.0",
    "@wdio/mocha-framework": "^9.0.0",
    "@wdio/spec-reporter": "^9.0.0"
  }
}
